import 'package:flutter/material.dart';

class LoginWebPage extends StatelessWidget {
  final TextEditingController userID;
  final TextEditingController userPassword;
  const LoginWebPage({super.key,required this.userID,required this.userPassword});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}